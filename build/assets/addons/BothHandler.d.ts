import CustomClient from "../classes/Client";
import { Bot } from "mineflayer";
declare const _default: (client: CustomClient, bot: Bot, username: any, message: string) => void;
export default _default;
