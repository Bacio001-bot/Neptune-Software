import CustomClient from "../classes/Client";
import { Bot } from "mineflayer";
import { Message } from "discord.js";
declare const _default: (client: CustomClient, bot: Bot, message: Message) => void;
export default _default;
